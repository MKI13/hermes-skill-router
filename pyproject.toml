[project]
name = "hermes-skill-router"
version = "0.2.1"
description = "Profile-scoped skill planning for Hermes Agent and OpenViking."
requires-python = ">=3.11"
license = { text = "MIT" }

[project.optional-dependencies]
dev = ["pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
